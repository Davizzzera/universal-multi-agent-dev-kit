# Web App Pack: Example Prompt

**Copy and paste the following into Antigravity:**

```text
Goal: Create a new dashboard widget showing recent transactions.
Pack: `web-app-pack`
Workflow: `coordinate`

Scope: `src/components/Dashboard/` and `src/api/transactions/`.
Instructions:
1. Use the Backend Specialist to create the API endpoint returning JSON data.
2. Use the Frontend Specialist to create the React component to consume the data.
3. Use the QA Tester to write tests for both.
Validation: Run `npm run test` and provide the output showing all tests pass.
Commit: Do not commit until I review the validation output.

Language Rule: Answer me in Brazilian Portuguese, but write repository files in English.
```
